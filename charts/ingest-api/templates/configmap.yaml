apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ingest-api.fullname" . }}
  labels:
    {{- include "ingest-api.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.config }}
  {{$key}}: {{ $value | quote }}
{{- end }}
