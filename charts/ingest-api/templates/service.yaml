apiVersion: v1
kind: Service
metadata:
  name: {{ include "ingest-api.fullname" . }}
  labels:
    {{- include "ingest-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ingest-api.selectorLabels" . | nindent 4 }}
