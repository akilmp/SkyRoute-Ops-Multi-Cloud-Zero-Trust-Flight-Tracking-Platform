apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ingest-api.fullname" . }}-test-connection"
  labels:
    {{- include "ingest-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ingest-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
