apiVersion: v1
kind: Service
metadata:
  name: {{ include "enrich-weather.fullname" . }}
  labels:
    {{- include "enrich-weather.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enrich-weather.selectorLabels" . | nindent 4 }}
