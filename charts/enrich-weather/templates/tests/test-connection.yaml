apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enrich-weather.fullname" . }}-test-connection"
  labels:
    {{- include "enrich-weather.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enrich-weather.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
